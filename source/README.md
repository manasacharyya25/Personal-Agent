# Archived learning code

These files are the FastAPI / RAG experiments from the repo root. They are not part of the Phase 1 app tree. Kept here so nothing is lost.

Root copies were left in place as well (nothing deleted).
